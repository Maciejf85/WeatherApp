class CityLocation {
     constructor(name, region_name, lat, lon) {
          this.name = name,
               this.region_name = region_name,
               this.lat = lat,
               this.lon = lon
     }
}

export default CityLocation;